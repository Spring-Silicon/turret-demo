[Service]
# Let the backend drain inference and close the worker's stdin before reaping
# children. Sending SIGTERM to the whole cgroup can interrupt a live GPU kernel.
KillMode=mixed
TimeoutStopSec=45
